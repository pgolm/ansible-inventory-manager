<%= render 'shared/form_errors', errors: @group.errors %>

<%= form_for [@group.inventory, @group] do |f| %>
  <%= render 'form', f: f %>

  <%= render 'shared/form_submit_cancel', f: f, back_path: @group.inventory %>
<% end %>