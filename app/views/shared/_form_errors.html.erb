<% if errors.any? %>
  <div class="alert alert-danger">
    <strong>Please fix the following <%= pluralize(errors.count, "error") %> :</strong>
 
    <ul>
    <% errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
